layout (location = 0) in vec3 aPos;

out vec3 TexCoords;
uniform mat4   mgl_PMVMatrix[2];


void main()
{
    TexCoords = aPos;
    gl_Position = mgl_PMVMatrix[0] * mgl_PMVMatrix[1] * vec4(aPos, 1.0);
}